<h2 class="text-center">Administration des invités</h2>
<div class="row main_grey">
  <div class="col-xs-12 col-sm-10 col-md-8 col-lg-6 col-sm-offset-1 col-md-offset-2 col-lg-offset-3">
    <%= link_to "Réponses", responses_admin_users_path %>
    <%= link_to "Messages", messages_admin_users_path %>
    <table class="table table-condensed">
      <thead>
        <tr>
          <th>Nom</th>
          <th>Prénom</th>
          <th>Rue</th>
          <th>Complément</th>
          <th>Code postal</th>
          <th>Ville</th>
          <th>Message</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
        <tr class="<%= user.response? ? user.comes? ? "success" : "error" : nil %>">
          <td><%= user[:lastname]%></td>
          <td><%= user[:firstname]%></td>
          <td><%= user[:street]%></td>
          <td><%= user[:address_add]%></td>
          <td><%= user[:zip_code]%></td>
          <td><%= user[:town]%></td>
          <td><%= user[:message_address]%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>